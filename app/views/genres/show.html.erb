<h2><%= @genre.name %></h2>
<h2>Books In This Genre:</h1>
<ol>
  <% @genre.books.each do |book| %>
    <li>Title: <%= link_to book.title, book_path(book) %><br>
    Author: <%= link_to book.author.name, author_path(book.author) %><br><br>
    </li>
  <% end %>
</ol>
